//Creamos la función salido que recibirá un string por parametros
function saludo(nombre) {
  //devolvemos hola + el nombre que llega por parametros
  return "Hola " + nombre;
}

//Exportamos la función saludo
module.exports = saludo;
